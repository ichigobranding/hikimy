<% if @error_message %>
<%= @error_message %>
<% end %>

<h2 class="post-title">ログイン</h2>

<%= form_tag("/login") do %>

  <div class="post-preview">
   <p>メールアドレス</p>
    <input name="email" value="<%= @email %>">
    <p>パスワード</p>
    <input type="password" name="password_digest" value="<%= @password_digest %>">
    <input type="submit" value="ログイン" >

<% end %>

  </div>

  <hr>